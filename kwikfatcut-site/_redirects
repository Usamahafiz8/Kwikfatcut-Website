# Cloudflare redirects — fixes the legacy URLs Google still has on file.
# Verified against Search Console "Not found (404)" and "Server error (5xx)" reports, 17 Jul 2026.

# --- "Not found (404)" group — both confirmed fixed & live ---
/blog             /learn    301
/blog/*           /learn    301
/learn-hub        /learn    301
/learn-hub/*      /learn    301

# --- Dead CTA URL from the old site (listed under 5xx in GSC, now returns 404) ---
/start-free       /              301
/start-free/*     /              301

# NOTE: the www.kwikfatcut.com 522 errors are NOT fixable from this file.
# They need a Cloudflare Redirect Rule (www -> non-www) at the zone level,
# because www never reaches this Worker at all.
